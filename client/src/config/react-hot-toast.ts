import { ToastOptions } from 'react-hot-toast';

const toastOptions: ToastOptions = {
  duration: 3000,
  position: 'top-right',
  className: '!text-sm !shadow !px-4 !py-2 !rounded-lg',
};

export { toastOptions };
